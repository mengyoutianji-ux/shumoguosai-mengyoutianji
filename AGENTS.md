# 数学建模工作流项目规则

本文件适用于仓库内全部目录。

## 权限与事实边界

- 用户最新指令优先于本仓库默认规则；题目原文和原始附件优先于二手总结。
- 区分原始记录、已验证计算、解释性推断、不确定项、建议与待办，不得将计划写成已完成结果。
- 保留原始文件，所有转换、修订和最终成果写入单独目录。未经明确授权，不覆盖、删除或发布材料。
- 页面、字段、样本量、模型结果、统计显著性和验证状态必须由当前项目文件或实际运行结果支持。

## 环境与产物

- 首选 Python 路径由 `config/workflow.yml` 指定。运行前先验证其存在；缺失时报告，不联网安装库。
- Excel 文件名、工作表名和字段标题使用中文，并写明必要单位。代码内部变量名可使用英文。
- 论文用图采用中文文件名，至少 300 dpi；图内不重复论文图题。
- 核心计算、参数和结果表默认保留四位小数。正文缩写精度时，必须保持同一指标前后一致。
- SHA256 只用于最终交付、关键原始数据或版本易混淆的成果，不对中间文件反复计算。
- 中间工作非必要不生成 PDF；LaTeX 最终稿必须编译 PDF 并逐页检查实际版面。

## 建模与统计

- 每问正文第一段必须用 3 至 4 行说明真实执行的数据预处理，包括样本量、变量和单位、缺失与异常检查、变量变换、排除记录及理由。
- 先明确题目要求回答的变量、指标和判据，再选择模型。不得通过堆叠模型掩盖结论不清。
- 关系结论使用正相关、负相关、不显著相关、强相关、弱相关等专业术语，并由合适的统计指标支持。非单调关系单独说明峰值、谷值或分段结构。
- 简单模型优先。复杂模型只有在样本外预测、物理合理性或稳定性明显改善时保留。
- 拟合优度接近理论上限时，检查计算错误、数据泄漏、样本量、参数数量和过拟合，并进行样本外或留一验证。
- 每问结束时直接回答题目，写清方向、强弱、关键数值、约束、例外和验证证据。

## 论文、表格与图表

- 摘要按“模型与算法建立过程、最终结果、结果与过程的合理性验证”组织，每问突出 3 至 4 个重点。
- 正文按“目标—预处理—模型—求解—结果—验证—结论”推进，避免模板化过渡和防御性结论。
- 每段最多加粗 3 至 5 个关键词，只加粗核心方法、模型名称和关键结果。
- 表格水平与垂直居中，正文必须解释表格和图中的趋势、差异、异常及建模意义。
- 图表风格统一，注释与数据范围匹配，阴影或区间不得无依据覆盖整个坐标轴。

## 验证与交付

- 运行代码并核对摘要、正文、表格、图表和附件中的关键数字、单位及精度。
- 对 Word 或 LaTeX 交付物进行实际渲染检查；结构检查不能替代逐页视觉检查。
- 交付前运行 `scripts/validate_repository.py` 及项目最终检查清单。
- 未执行的测试、无法读取的材料和仍存在的风险必须明确说明。
