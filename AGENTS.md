# 数学建模工作流根入口

本文件适用于仓库内全部目录，也是处理本仓库及其生成项目时必须首先读取的入口。

## 启动顺序

1. 读取本文件。
2. 读取 [`skills/mathematical-modeling-workflow/SKILL.md`](skills/mathematical-modeling-workflow/SKILL.md)。
3. 读取 [`docs/规则版本与冲突处理.md`](docs/规则版本与冲突处理.md) 和 [`docs/工作流总览.md`](docs/工作流总览.md)。
4. 按技能入口的任务路由读取对应专题规范；不得只读本文件后凭通用经验执行。
5. 开始建模前清点并阅读用户提供的全部材料，尤其是题目原文、附件和最新补充要求。存在会改变模型含义或交付形式的疑问时，先报告疑问。

## 规则优先级

- 用户在当前任务中的最新明确要求；
- 题目原文、官方附件和提交规范；
- 当前项目中经过实际运行或核验的事实；
- [`config/workflow.yml`](config/workflow.yml) 标注的当前版本规则；
- 对应专题规范；
- 本文件和总览中的通用默认值。

同一层级发生冲突时，以明确标注的较新生效日期或较高版本号为准，不以文件修改时间、文件名中的“最终版”或未经核验的口头转述判断。数值配置以 `config/workflow.yml` 为准，专题细节以对应 `docs/` 文件为准。

## 权限与事实边界

- 区分原始记录、已验证计算、解释性推断、不确定项、建议与待办，不得将计划写成已完成结果。
- 保留原始文件，所有转换、修订和最终成果写入单独目录。未经明确授权，不覆盖、删除、提交或发布材料。
- 页面、字段、样本量、模型结果、统计显著性和验证状态必须由当前项目文件或实际运行结果支持。
- 题目已经明确给出的条件不得改写为模型假设。假设只补充题目未规定且建模确实需要的条件。

## 环境与产物

- 代码保存在项目的 `03_模型与代码`，执行、依赖检查和结果生成固定使用 `config/workflow.yml` 指定的 Python。运行前先验证路径；不得调用系统 Python，也不得未经授权联网安装库。
- Excel 文件名、工作表名和字段标题使用中文，并写明必要单位。代码内部变量名可使用英文。
- 论文用图采用中文文件名，至少 300 dpi；图内不重复论文图题。流程图和建模示意图保留可编辑源文件。
- 核心计算、参数和结果表默认保留四位小数。正文缩写精度时，必须保持同一指标前后一致。
- 随机种子和软件版本保留在项目复现记录中，不在论文正文、摘要、表格或图中展示具体种子值。
- SHA256 只用于最终交付、关键原始数据或版本易混淆的成果，不对中间文件反复计算。
- 中间工作非必要不生成 PDF；LaTeX 最终稿必须编译 PDF 并逐页检查实际版面。

## 建模与写作底线

- 每问先明确目标、变量、约束、评价指标和判据，再选择模型；不得通过堆叠模型掩盖结论不清。
- 每个关键公式前后说明它建立的模型、变量关系、适用条件及其在算法中的作用，避免公式与叙述杂糅。
- 对决定结论成立的关键命题给出证明或严格论证，并把数学判据与数值实现逐项对应。示意图不能替代证明。
- 每问正文第一段准确说明实际数据核验或预处理；没有外部数据时改写为输入、单位和题设参数核验，不得虚构清洗步骤。
- 每问优先规划流程图、建模示意图、结果图和验证图。图表必须服务于明确论断，正文按“引出—呈现—分析”逐图闭环。
- 每问的直接结论放在模型结果部分，不使用装饰性边框；全文结尾只作必要汇总。

## 验证与交付

- 运行代码并核对摘要、正文、表格、图表和附件中的关键数字、单位及精度。
- 对 Word 或 LaTeX 交付物进行实际渲染检查；结构检查不能替代逐页视觉检查。
- 交付前运行 `scripts/validate_repository.py` 及项目最终检查清单。
- 未执行的测试、无法读取的材料和仍存在的风险必须明确说明。
