# 参与改进

本工作流允许持续迭代。提交改进前，请说明：

1. 当前规则或流程造成了什么可复现问题；
2. 该问题适用于哪些题型、软件或交付格式；
3. 建议修改的规则、模板或脚本；
4. 如何验证修改确实改善了结果；
5. 是否会与现有用户要求、题目约束或发布边界冲突。

稳定且通用的规则写入 `docs/`，用户可调参数写入 `config/workflow.yml`，具体题目的数据、模型结果和内部材料不得提交到本仓库。

提交前运行：

```powershell
py .\scripts\validate_repository.py
```
